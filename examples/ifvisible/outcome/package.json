{
  "name": "ifvisible.js",
  "version": "1.0.0",
  "description": "Cross-browser, lightweight way to check if user is looking at the page or interacting with it.",
  "main": "dist/ifvisible.js",
  "types": "dist/ifvisible.d.ts",
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc --emitDeclarationOnly && mv dist/index.d.ts dist/ifvisible.d.ts"
  },
  "keywords": [
    "visibility",
    "idle",
    "activity",
    "ui"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
